apiVersion: v1
kind: Service
metadata:
  name: driftix-service
  labels:
    app: driftix
spec:
  type: LoadBalancer
  ports:
  - name: frontend
    port: 80
    targetPort: 3000
    protocol: TCP
  - name: backend
    port: 3000
    targetPort: 3000
    protocol: TCP
  selector:
    app: driftix 